{
  "name": "ipro-portal-e2e",
  "private": true,
  "version": "0.0.1",
  "description": "iPro Portal — E2E test runner (Playwright). El backend y el frontend viven en sus propias carpetas con sus propios package.json; este root SOLO existe para correr Playwright contra ambos.",
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install --with-deps chromium",
    "e2e:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^16.4.5",
    "pg": "^8.12.0",
    "speakeasy": "^2.0.0"
  },
  "engines": {
    "node": "20.x"
  }
}
