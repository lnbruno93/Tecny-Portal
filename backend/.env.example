PORT=3001

# PostgreSQL
DATABASE_URL=postgresql://user:password@localhost:5432/ipro_portal

# JWT
JWT_SECRET=change_this_to_a_strong_random_secret
JWT_EXPIRES_IN=7d

# CORS — URL del frontend (Index.html)
CORS_ORIGIN=http://localhost:5500
